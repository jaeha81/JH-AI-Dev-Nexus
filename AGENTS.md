# Codex Startup Pointer

Keep this file short to avoid startup context bloat.

Before planning, Goal Mode work, handoff, sync, or Agent Room logging, read only the relevant section of:

- `codex-knowledge/context-management.md`

Default rule: use LLM Wiki, Obsidian summaries, and Agent Room records as context compression layers. Start narrow, then expand only when evidence is missing.

Multi-PC rule: assume the user may continue from another PC. Keep Codex startup rules portable and pointer-based, then record handoff/sync-relevant changes in the proper knowledge or Agent Room channel.
